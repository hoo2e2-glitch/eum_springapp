<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.app.springapp.mapper.TimeMapper">

    <select id="selectAll" resultType="EduResponseDTO">
        SELECT
            ID AS id,
            EDU_TITLE AS eduTitle,
            EDU_DETAIL AS eduDetail,
            EDU_DIA AS eduDia
        FROM TBL_EDU
        WHERE EDU_IS_DELETED = 0
        ORDER BY ID DESC
    </select>

    <select id="select" parameterType="Long" resultType="EduResponseDTO">
        SELECT
            ID AS id,
            EDU_TITLE AS eduTitle,
            EDU_DETAIL AS eduDetail,
            EDU_DIA AS eduDia
        FROM TBL_EDU
        WHERE ID = #{id}
          AND EDU_IS_DELETED = 0
    </select>

</mapper>