<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.app.springapp.mapper.QuizStartMapper">

    <!-- 퀴즈 시작 기록 조회 -->
    <select id="selectByUserIdAndQuizId" resultType="QuizStartVO">
        SELECT ID, QUIZ_START_AT, USER_ID, QUIZ_ID
        FROM TBL_QUIZ_START
        WHERE USER_ID = #{userId}
          AND QUIZ_ID = #{quizId}
    </select>

    <!-- 퀴즈 시작 기록 등록 -->
    <insert id="insert" parameterType="QuizStartVO">
        <selectKey keyProperty="id" resultType="Long" order="BEFORE">
            SELECT SEQ_QUIZ_START.NEXTVAL FROM DUAL
        </selectKey>
        INSERT INTO TBL_QUIZ_START(ID, USER_ID, QUIZ_ID)
        VALUES(#{id}, #{userId}, #{quizId})
    </insert>

    <!-- 퀴즈 시작 시간 갱신: 다시 퀴즈를 시작하면 진행중 상태로 갱신 -->
    <update id="updateStartAt">
        UPDATE TBL_QUIZ_START
        SET QUIZ_START_AT = CURRENT_TIMESTAMP
        WHERE USER_ID = #{userId}
          AND QUIZ_ID = #{quizId}
    </update>

</mapper>